server:
  port: 8200
  error:
    include-message: always

spring:
  application:
    name: shop-service
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: "jdbc:postgresql://localhost:5432/shopme"
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: shop
  main:
    allow-circular-references: true

keycloak:
  auth-server-url: http://localhost:8180/auth/
  realm: shop-admin
  resource: shop-client
  credentials:
    secret: JL5tZUYEG0X6ZyqFvyHdLfDybtAF41UA